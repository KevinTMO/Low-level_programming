#include <stdio.h>

/**
*main - will print all digits of base10
*Return: will return 0 if success
*/

int main(void)
{
	int num0 = 0;

	while (num0 <= 9)
	{
		printf("%d", num0);
		num0++;
	}
	printf("\n");
	return (0);
}
