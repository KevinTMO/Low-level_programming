#include "roadmap.h"

/**
*strLen - will return the lenght of the str received
*@str: the string of characters
*Return: will return the length
*/

int strLen(char *str)
{
	int iter = 0;

	for (iter = 0; str[iter] != '\0'; iter++)
	{
		/* Nothing happening here... move on. */
	}
	return (iter);
}
