#ifndef ROADMAP_H
#define ROADMAP_H

/* STD LBS */

#include <stdio.h>

/* PROTOTYPES */

void _memset(char *ptr, int chr, size_t size); /* Will set the memory address of an array with a value */

void _memcpy(char *dest, const char *src, size_t size); /* Will copy the memory block src to dest */

#endif