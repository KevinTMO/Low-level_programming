#include "roadmap.h"

/**
*main - will call printT98() to print all natural numbers from n to 98
*Return: will return 0 if success
*/

int main(void)
{
        printTo98(0);
        printTo98(-20);
        printTo98(138);
        printTo98(1024);
        printTo98(98);
        return (0);
}