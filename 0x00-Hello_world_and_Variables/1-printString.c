#include <stdio.h>

/**
*main - use printf() to print a string followed by a newline
*Return: will return 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\n");

	return (0);
}
